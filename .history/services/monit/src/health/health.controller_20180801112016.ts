export class HealthController {
  apiConfig: Array<any>;

  constructor(config: Array<any>) {
    this.apiConfig = config;
  }

  collectHealthInfo: fucntion() {
    if (this.apiConfig.length) {

    }
  }
}

